# Minimal dependencies for deploying the Theorem Codex web dashboard
# (components/theorem_codex/apps/streamlit_app.py), e.g. on Streamlit
# Community Cloud. For the full local toolchain use requirements-unified.txt.
streamlit>=1.45
pandas>=2.0
# Required for Streamlit's native OIDC login (st.login / st.user).
Authlib>=1.3.2
